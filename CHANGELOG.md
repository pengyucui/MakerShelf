# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的结构，并使用[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

暂无。

## [2.0] - 2026-09-23

### 新增

- 增加 OBJ 单色静态几何预览和封面候选，复用 STL 渲染、缓存、重试与日志；支持正负面索引，凹多边形给出明确错误。
- 新增「运行日志」页，提供搜索候选、统计卡片和可展开记录；支持级别 / 模块 / 关键词 / 本次运行筛选、实时刷新、复制、导出及确认清空。
- 新增串行本地日志服务，覆盖应用、预览、图片、导入、下载、存储和账号关键事件；最近 2,000 条内存记录、三份各不超过 2 MiB 的轮转文件及常见凭据脱敏。
- 新增二进制及 ASCII STL 静态几何预览、缩略图放大和本地封面候选；分块多次读取并绘制完整网格，按预算调整分辨率，失败原因写入日志。
- 设置页新增 STL 预览候选像素预算与最长处理时间，可恢复默认；参数变化后使用新的预览缓存。
- 文件预览失败提供主动重试，跳过失败缓存；补齐文件读取和缓存写入错误提示。

### 界面调整

- 重构原生 SwiftUI 窗口，统一浅色卡片、深墨色按钮与毛玻璃侧栏，并适配深色外观。
- 删除侧栏重复的来源筛选；模型库顶部集中管理来源、状态、排序，作者与分类放入更多筛选。
- 模型封面统一为 4:3，信息区保持一致；详情可关闭，窄窗口使用原生详情弹窗。
- 新增底部下载摘要，支持暂停、继续与重试；卡片使用独立的任务状态视图。
- 统一下载、设置、导入和本地编辑页面样式，缩略图增加 320 像素尺寸档位。
- 详情接入完整图文介绍，优先读取本地图片并过滤 HTML 包装；操作按钮改为纵向排列，当前任务状态与卡片一致。
- 窄窗口详情关闭后再进入编辑或下载任务页；已存在任务时提供队列入口。
- 下载摘要忽略同模型已被替代的旧任务，暂停全部仅在有可暂停任务时启用，失败与部分完成操作明确标为重试。
- 编辑站点模型的预览保留来源名称；图片缓存及在途请求加入归档版本，避免同路径更新后复用旧图。
- 版本号升至 `2.0`（构建 11）；自动发布资产明确标注为未签名构建。
- 登录钥匙串改为先做无界面检查。旧记录只在第一次读取时确认一次，然后删除并用当前 App 重新创建，避免每次打开设置都改访问权限。
- 详情里的 3MF 文件行显示包内打印盘预览；没有内嵌预览时显示文件格式占位。
- 新下载的展示图用 libwebp 收成最长边 1600 像素、质量 75 的 WebP；失败依次回退 JPEG、原文件。成功转码的动图只保留第一帧。
- 添加本地模型时，如果拖入或选择的 3MF 带有包内预览，会询问是否把它用作封面。

### 修复与优化

- 修复 STEP / STP 文件行借用 STL 模型封面的错图；文件预览仅归属于当前文件，编辑替换文件或归档版本变化后重置状态。
- STL / OBJ 完整网格渲染替代抽面，避免人工丢面造成孔洞；按实际投影居中，多方向光照，优先 960 × 720，预算允许时使用两倍超采样抗锯齿。升级缓存版本以隔离旧图片。

- 修复同一 App 重启后反复请求钥匙串授权：缺少数据保护钥匙串授权的构建改用本机登录钥匙串，为当前 App 保存持久访问权限，不再重复执行失败的迁移；账号页增加重新保存入口，断开连接不再忽略删除失败。
- 修复钥匙串迁移在新条目写入失败时仍删除旧记录的问题；保留系统错误码、账号页提示和运行日志，设置页恢复任务防重入。
- 日志搜索候选改用锚定输入框的系统浮层，修复列表可能被筛选栏或日志滚动区域遮挡、裁切的问题。
- 移除 STL 旧版 64 MiB / 50 万面限制；默认资源预算仍待真实大模型的内存和耗时实测，OBJ 原有限制保持不变。
- 3MF 预览改为按范围读取，增加目录和解压大小限制、本地文件头及 CRC 校验；缓存按文件版本区分并限制数量。
- 图片压缩移入串行 actor，修复 RGBA 指针使用范围，编码完成检查取消后再落盘；下载临时路径相互隔离，所有退出路径清理临时文件。
- 图片归档名包含来源摘要，继续任务同时复用 JPEG 和原格式回退；保留旧图以免中断更新破坏已有介绍引用。
- 封面提取任务统一由表单状态管理，修复旧结果回写、临时图片残留、搜索后误选隐藏项及候选列表溢出；使用原生弹窗处理键盘操作。
- 文件行随归档目录和模型版本重新加载预览；libwebp 许可加入应用资源，依赖说明与当前工程保持一致。

### 开源整理

- docs 仅保留发布流程和功能梳理，取消其他内部资料的跟踪并增加忽略规则。
- 发布说明迁移至 `.github/release-notes/`，同步更新工作流、README 与贡献指南。
- 随仓库提供 libwebp 源码与许可，修正依赖说明。

### 验证说明

- 本次发布整理未在本机重新编译或运行测试；安装包由 GitHub Actions 构建。
- 未签名、未公证；真实账号下载、大模型效果与性能仍需在目标设备验收。

## [1.0] - 2026-09-22

### 新增

- 原生 SwiftUI macOS 模型库、下载任务和设置页面。
- MakerWorld 中文站与国际站网页登录、当前账号收藏和发布内容导入。
- 指定作者用户名、数字 UID 和主页链接解析，公开发布模型分页预览。
- 单模型链接导入、文件、介绍与展示图片完整归档。
- 本地模型创建、编辑、封面管理和文件快照归档。
- Native Glass 界面、统一尺寸卡片、网格与列表视图。

### 修复

- 修复作者公开列表错误复用网页登录令牌的问题。
- 修复国际站主页链接被默认中文站拒绝的问题。
- 修复模型图片比例不同导致卡片尺寸不一致的问题。
- 将钥匙串读取改为联网操作时按需恢复。

[未发布]: https://github.com/pengyucui/MakerShelf/compare/v2.0...HEAD
[2.0]: https://github.com/pengyucui/MakerShelf/compare/v1.0...v2.0
[1.0]: https://github.com/pengyucui/MakerShelf/releases/tag/v1.0
